{
  "$schema": "https://json-schema.org/schema",
  "$id": "CreateModuleCore",
  "title": "",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "Destination path under libs/core/src (e.g., 'article', 'ports', 'user/profile')",
      "x-prompt": "Where should the module be created? (relative to libs/core/src)"
    },
    "name": {
      "type": "string",
      "description": "The name of the module to create",
      "x-prompt": "What name would you like to use?"
    },
    "idField": {
      "type": "string",
      "default": "id",
      "description": "The name of the ID field for the module",
      "x-prompt": "What is the name of the ID field?"
    }
  },
  "required": ["path"]
}
